# sample_service — a fixture, not a product

A deliberately small tree with a realistic mix: one module that reaches the
network, one that does not, and one that uses cryptography. It exists so a
reader can run the auditors, get findings that are not trivially empty, and
compare their output to the report committed beside it.

Nothing here is shipped software. Do not import it.
